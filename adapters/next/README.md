# `@veritio/next`

Next.js adapter for route handlers, server actions, and request-scoped evidence.

The adapter will focus on server-side capture. Browser code should not receive storage credentials or Veritio provider tokens.
