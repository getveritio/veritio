/// <reference types="vite/client" />

// Vite's client types declare the `*.css` side-effect module used by main.ts
// (the shared design-kit stylesheet) and the `*.vue` SFC module shape so
// vue-tsc resolves both under the example's strict tsconfig.
