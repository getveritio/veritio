# `@veritio/server`

Self-hosted Node server surface for ingestion and querying.

The first server version should expose:

- event ingestion
- hash-chain verification
- retention policy inspection
- DSAR workflow APIs
- SIEM export jobs

Storage credentials must be configured by the server operator and never sent to browser clients.
