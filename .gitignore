node_modules
dist
.svelte-kit
.next
.output
.tanstack
.nitro
*.tsbuildinfo
.DS_Store
.env
.env.*
!.env.example
.claude/settings.local.json
__pycache__
.pytest_cache
.ruff_cache
.venv
*.pyc
*.egg-info/
coverage
*.test

# Local agent planning artifacts
docs/superpowers/
docs/proof-of-work/
.codex/private/

# Compiled example binaries (go build output named after the example dir)
examples/gin-governed-crud/gin-governed-crud

# Private strategy/brainstorming vault (Obsidian) — never committed
/strategy/

# Generated reports/artifacts — never committed (prevents accidental git add -A of
# ad-hoc HTML reports, cf. the PR #36 exposure)
/outputs/
